<% provide(:title, @category.name) %>

<div class="row">
  <section>
    <h1>
      <%= @category.name %>
    </h1>
    <h2>
      <%= @category.des %>
    </h2>
  </section>
</div>

<% if current_user.teacher? %>
  <%= render 'shared/word_form' %>
<% end %>

<ul class="users">
  <%= render @words %>
</ul>